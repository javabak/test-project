spring:
  datasource:
    password: Alimnikas299
    username: root
    url: jdbc:mysql://localhost:3306/test_db?useSSL=false&useTimezone=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database: mysql
    show-sql: true
    hibernate:
        ddl-auto: create-drop
    defer-datasource-initialization: true
    open-in-view: true

  sql:
    init:
      mode: always

logging:
  level:
    sql: info
    web: info

